/**
 * React コンポーネント — NavBar のテスト
 */
import { describe, it, expect } from "vitest";
import Component from "@/components/NavBar";


describe("NavBar", () => {
  it("テストスイートが定義されていること", () => {
    expect(true).toBe(true);
  });
});
