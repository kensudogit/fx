/**
 * React コンポーネント — TradingViewWidget のテスト
 */
import { describe, it, expect } from "vitest";
import Component from "@/components/TradingViewWidget";


describe("TradingViewWidget", () => {
  it("テストスイートが定義されていること", () => {
    expect(true).toBe(true);
  });
});
