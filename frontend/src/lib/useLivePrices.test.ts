/**
 * フロントエンド共通ライブラリ — useLivePrices のテスト
 */
import { describe, it, expect } from "vitest";

describe("useLivePrices", () => {
  it("テストスイートが定義されていること", () => {
    expect(true).toBe(true);
  });
});
